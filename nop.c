#include "monty.h"

/**
 * dinop - does nothing
 * @top: pointer to the head of the stack
 * @line_number: line number of the command being run
 */
void dinop(stack_t **top, uint line_number)
{
	(void)top;
	(void)line_number;
}
